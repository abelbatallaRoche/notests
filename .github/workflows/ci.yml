name: CI

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop

jobs:
  install:
    name: install
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Configure npm registry
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Debug - Check paths before install
        run: |
          echo "Current directory: $(pwd)"
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          echo "Absolute path to node_modules will be: $(pwd)/node_modules"

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        run: npm install --no-audit

      - name: Debug - Check paths after install
        run: |
          echo "node_modules created at: $(pwd)/node_modules"
          echo "Number of packages installed:"
          ls -1 node_modules | wc -l

  typecheck:
    name: typecheck
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Run typecheck
        run: npm run typecheck

  lint:
    name: lint
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Run lint
        run: npm run lint

  format:
    name: format
    runs-on: ubuntu-latest
    needs: install
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Run format check
        run: npm run format:check

  build:
    name: build
    runs-on: ubuntu-latest
    needs: [install, typecheck, lint, format]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Build
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 7

  unit_tests:
    name: unit tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Run tests
        run: npm run test -- --run

      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: coverage/
          retention-days: 7

  e2e_tests:
    name: e2e tests
    runs-on: ubuntu-latest
    needs: build
    container:
      image: mcr.microsoft.com/playwright:v1.57.0-noble
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Debug - Check paths before cache restore
        run: |
          echo "Current directory: $(pwd)"
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          echo "Checking if node_modules exists:"
          ls -la node_modules || echo "node_modules does not exist"
          echo "Checking /github/workspace:"
          ls -la /github/workspace/ || echo "/github/workspace does not exist"

      - name: Restore cache
        id: cache
        uses: actions/cache/restore@v4
        with:
          path: /github/workspace/node_modules
          key: npm-cache-${{ hashFiles('package-lock.json') }}

      - name: Debug - Check paths after cache restore
        run: |
          echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"
          echo "Current directory: $(pwd)"
          echo "Checking if node_modules exists now:"
          ls -la node_modules || echo "node_modules still does not exist"
          echo "Checking /github/workspace/node_modules:"
          ls -la /github/workspace/node_modules || echo "/github/workspace/node_modules does not exist"
          echo "Number of packages in node_modules:"
          ls -1 node_modules 2>/dev/null | wc -l || echo "0"

      - name: Configure npm registry
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          npm config set @oneds:registry=https://eu.repository.roche.com/artifactory/api/npm/oneds-npm/
          npm config set //eu.repository.roche.com/artifactory/api/npm/oneds-npm/:_authToken=${{ secrets.NPM_TOKEN }}

      - name: Install dependencies (if cache miss)
        if: steps.cache.outputs.cache-hit != 'true'
        run: npm ci --no-audit

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 7
